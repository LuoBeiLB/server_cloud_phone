# 项目进度 STATUS

> 最后更新：2026-07-27 ｜ 分支 `main` ｜ 60 次提交 ｜ 17 前端视图 · 18 后端路由 · 25 证据截图
> **一句话**：本月 Demo 主流程 **8/8 验收全绿**（真机已验证），并已大幅超额实现全量方案中的十余项功能；距"生产级 500 台 + 系统级 iOS ROM"仍有明确差距（见 §6）。

---

## 1. 进度总览

| 维度 | 目标 | 现状 | 完成度 |
|---|---|---|---|
| 本月 Demo 主流程（8 条验收） | 主流程跑通 | 8/8 全绿，真机复验 | ✅ 100% |
| 甲方五要素 | 五要素齐备 | 全部有交付物 | ✅ 100%（demo 级） |
| 真机（Redroid） | 浏览器能开网页 | Android 12 arm64 原生跑通，10 台密度实测 | ✅ |
| 全量方案功能（看板/权限/告警/报表…） | 24 周全量 | 已实现 ~12 项子集 | 🟡 ~40% |
| 规模化 500 台 / k8s | 500 台 | 已出 k8s 预研（manifests + 容量估算 500 台需 12–31 节点，未上真集群） | 🟡 预研完成 |
| 系统级 iOS ROM | ≥90% 复刻 | 皮肤级（壁纸/图标圆角/网格/Dock） | 🟡 皮肤级 |
| 硬件 GPU 加速 | 生产渲染 | VM 软件渲染(SwiftShader) | 🔴 需裸机 |

图例：✅ 达成 ｜ 🟡 部分 ｜ 🔴 未开始

---

## 2. 甲方五要素 交付状态

| 要素 | 交付物 | 状态 |
|---|---|---|
| x86 虚拟云手机 | Redroid Android12 + Chromium/WebView + 一机一码 | ✅ 真机跑通 |
| 苹果 UI | 高保真原创 iOS 界面（主屏/锁屏/控制中心/Safari，SVG 矢量，App 内可查看）+ 真机 Lawnchair 皮肤 | ✅ 皮肤级高保真（原创，无 Apple 素材） |
| 群控投屏客户端 | Web 多画面预览 + 单机操控 + 同步(1控N) + scrcpy H.264 | ✅ |
| App 客户端 | 安卓 Kotlin/Compose（登录/列表/投屏/批量） | 🟡 骨架（未 Gradle 出包） |
| 网站登录及管理系统 | Vue3 控制台（13 项菜单） | ✅ |

---

## 3. 本月 Demo · 8 条验收

| # | 验收项 | 状态 | 证据 |
|---|---|---|---|
| 1 | 登录 | ✅ | `routers/auth.py` |
| 2 | 建机(iOS皮肤) | ✅ | `真机_iOS皮肤.png`、`规模化_10台真机网格.png` |
| 3 | 开网页 | ✅ | `真机截图_经平台API.png`（WebView125 渲染 example.*） |
| 4 | 独立身份 | ✅ | IP/AndroidID/UA 各不同（实测） |
| 5 | 投屏预览 | ✅ | 10 宫格真机 PNG 帧 |
| 6 | 单台操控 | ✅ | 往返 ~4–90ms，预览刷新 ~400ms、`实时验证_切到org.png` |
| 7 | 批量同步 | ✅ | 一键开网页 10/10，1.9s |
| 8 | App+脚本 | ✅ | 脚本跨 10 台回放 10/10；App `app-client/` |

一键复现：
- simulator：`cd backend && python -m tests.smoke`（会批量建 10 台；拒绝打真机后端）
- 真机：`cd backend && python acceptance_live.py`（**非破坏性**，只用现有在跑的设备核验，
  跑完还原原网页；验收 2「建机 10 台」属破坏性操作不代跑，不足 10 台记 SKIP）

---

## 4. D1–D20 逐日进度（本月计划）

W1 云手机底座（D1–5）✅ · W2 群控+Web管理（D6–10）✅ · W3 批量+脚本+App（D11–15）✅ · W4 PC+集成+演示（D16–20）✅（D19 稳定性仅密度实测，未跑满 2h；见 §6）。详见 `docs/plan-mapping.md`。

---

## 5. 已实现功能全清单

**核心主流程**：登录鉴权 · 设备增删启停 · 一机一码(设备标识+浏览器指纹+独立出口IP) · iOS皮肤 · 多画面预览 · 单机操控 · 批量/同步(1控N) · 脚本录制回放 · 实时预览(WS+HTTP轮询)。

**扩展模块（超出 demo 范围，全量方案子集）**：
数据看板 · 告警监控 · 任务调度(定时/循环) · 设备详情+已装应用 · 统计报表(CSV导出) · 设备日志(logcat) · 四级权限RBAC+操作审计 · 应用管理(装/卸/启/停/清) · 多级嵌套分组+拖拽 · 文件互传(上传/下载/删除) · 分辨率/DPI切换(CP-007) · 低延迟投屏(高清12fps + ws-scrcpy H.264) · 高保真原创 iOS 皮肤 7 屏(主屏/今天/资源库/搜索/锁屏/控制中心/Safari) · **一键换肤(3 套原创主题·每台按钮/批量下发·预览即时+真机桌面与设计稿 1:1：设计图标 BLOB/中文标签/Dock 毛玻璃底板/页面圆点/分档壁纸随设备轮换·WS 实时进度提示)** · 规模化 k8s 预研 · PC(Electron)/App(Kotlin)客户端 · 一键真机接入脚本 · 容器自启+持久化DB加固。

---

## 6. 与目标的差距（Gap）

### 本月范围内——已全部达成 ✅
8 条验收全绿；五要素齐备（demo 级）；真机跑通并密度实测。

### 全量方案（后续路线图）——待办 🔴/🟡
| 项 | 目标 | 差距 | 优先级 |
|---|---|---|---|
| 规模化 500 台 | k8s 编排 500 台 | 已出 k8s manifests + 规模化方案（`docs/规模化方案.md`）；未在真集群验证 | 高 |
| 稳定性 | 500 台 72h/99.5% | 仅密度实测，无 2h+ 持续压测 | 高 |
| 系统级 iOS ROM | SystemUI/控制中心/锁屏/设置复刻 | 已有高保真原创皮肤（主屏/锁屏/控制中心/Safari），但为渲染层/皮肤级；真机系统级 ROM（AOSP SystemUI fork）未做 | 中 |
| 硬件 GPU 加速 | 生产渲染 | VM 软件渲染；需 Intel/AMD 裸机 Mesa | 中 |
| App 出包 | 安卓 APK | 仅 Kotlin 骨架，未 Gradle 构建 | 中 |
| 深度风控对抗 | 反检测 | 未做（长期运维项，合规敏感） | 低/合规 |
| 传感器模拟(CP-008) | GPS/陀螺仪注入 | 未做（redroid 注入路径待验证） | 低 |
| 真机 iOS 图标包 | 桌面用原创 iOS 图标 | 需打图标包 APK（缺 Android SDK 工具链），阻塞 | 低 |
| ws-scrcpy 内嵌 | 同域反代+统一鉴权+iframe | 当前新窗口打开 | 低 |
| 硬件/带宽/IP代理/等保 | 甲方承担 | 不在开发范围 | — |

---

## 7. 真机实测指标（redroid on Apple Silicon）

Android 12 arm64 原生、`native.bridge=0`（零转译）；单实例 ~606 MiB；adb 往返 7ms、tap 33ms；8G VM 密度上限 ≈ 10 台；GL 软件渲染(SwiftShader/ANGLE OpenGL ES 3.1)；Chromium WebView 125；scrcpy H.264 720x1280 已验证（CLI + ws-scrcpy 浏览器 Broadway 解码）。详见 `docs/redroid-real-benchmark.md`。

---

## 8. 当前运行态

- Lima VM `redroid` Running；后端 **redroid 模式** :8000（systemd `cloudbe`），Mac 经 ssh 隧道接入；ws-scrcpy :8100（systemd `wsscrcpy`）+ 隧道；前端 vite :5173。
- 真机 **5 台运行中**（DB 持久化 `/root/cloud-data`，容器 `restart_policy=unless-stopped`）。
- 登录 admin/admin123；菜单 13 项。
- 恢复/管理见 `docs/演示脚本.md` §0 与 `deploy/scripts/connect-vm-redroid.sh`。

---

## 9. 已知限制 / 风险

- **主机休眠会停 Lima VM**：已加固（容器自启+持久 DB），但 VM 重启后需重连隧道、重启 ws-scrcpy；且 root adb server 偶发卡住（重启 adb 即可）。
- **设备 adb 掉线**：平台已能自愈（操作时自动重连重试，~2s）；重连也失败才报 503 并在
  管理台明确提示。日常巡检建议跑 `acceptance_live.py`（v1.1 前该场景会静默降级成
  「画面正常但设备其实失联」，已修复，详见 CHANGELOG v1.1）。
- **VM 内存 8G 上限 ≈ 10 台**：跑满 20 台需 ≥14G（Mac 16G 受限）。
- **图形软件渲染**：不代表生产裸机性能。
- **ARM 转译商用授权**：libndk/libhoudini 专有，规模化商用需书面确认（本项目用 64only 原生 arm，未依赖转译）。

---

## 10. 下一步（建议优先级）

1. **补齐 10 台真机跑满 8/8**（本次恢复仅 5 台）+ 2h+ 稳定性观察。
2. **规模化预研**：Docker Compose→k8s、多节点调度、批量部署脚本（对齐 500 台）。
3. **iOS 图标包层**：接入合规开源图标包，桌面真正 iOS 化。
4. **App Gradle 出包** + 三端真机联调录屏。
5. **ws-scrcpy 同域反代 + 内嵌操控页**。
6. 文件互传(PC-104)、传感器模拟(CP-008) 等零散项。

> 本文件随进度更新；每次里程碑后同步 §1 总览与 §6 差距表。
