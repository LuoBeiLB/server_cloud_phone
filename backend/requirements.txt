# X86 云手机平台 —— 后端依赖
# 说明：密码哈希使用标准库 hashlib.pbkdf2，避免 bcrypt 原生编译依赖。
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20          # dev 默认库（SQLite）
asyncpg>=0.29            # 生产库（PostgreSQL），按 DATABASE_URL 启用
pydantic>=2.7
pydantic-settings>=2.3
PyJWT>=2.8
python-multipart>=0.0.9
httpx>=0.27
# 可选：真实 Redroid 后端需要 Docker SDK（Simulator 模式无需安装）
docker>=7.1 ; platform_system == "Linux"
